<div>
  <% if place.favorited_by?(current_user) %>
      <%= link_to place_favorites_path(place), method: :delete, remote: true do %>
        ♥<%= place.favorites.count %>
    　<% end %>
  <% else %>
    <p>
      <%= link_to place_favorites_path(place), method: :post, remote: true do %>
        ♡<%= place.favorites.count %>
  <% end %>
  <% end %>
</div>